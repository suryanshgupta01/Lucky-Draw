import React from 'react'

const Color = ({ colour }) => {
    return (
        <span style={{ width: '40px', display: 'inline-block', height: '40px', borderRadius: '50%', backgroundColor: colour }}>
        </span>
    )
}

export default Color
